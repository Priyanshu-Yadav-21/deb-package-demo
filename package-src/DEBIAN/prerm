#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        echo "[prerm] Cleaning active demo-app instances before removal..."
        ;;
    failed-upgrade)
        ;;
    *)
        echo "[prerm] Called with unknown argument: $1" >&2
        exit 1
        ;;
esac

exit 0
